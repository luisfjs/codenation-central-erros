spring:
  application:
    name: central-erros
  datasource:
    username: root
    password: Suro3624#
    url: jdbc:mysql://0.0.0.0/centralerros?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  #security:
  #  user:
  #    name: lsena
  #    password: mudar@12
  #    role: ADMIN

jwt:
  config:
    login-url: /auth/login
    type: encrypted